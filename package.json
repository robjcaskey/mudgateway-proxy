{
  "name": "mudgateway-proxy",
  "version": "1.0.0",
  "description": "proxy to allow others to play MUDs over websocket",
  "main": "mudgateway-proxy.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bluebird": "^3.5.2",
    "requests": "^0.2.2"
  }
}
